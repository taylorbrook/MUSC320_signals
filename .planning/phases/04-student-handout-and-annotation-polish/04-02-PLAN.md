---
phase: 04-student-handout-and-annotation-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/01-hello-signal.maxpat
  - patches/02-first-tone.maxpat
  - patches/03-control-meets-signal.maxpat
  - patches/04-noise-and-amplitude.maxpat
  - patches/05-envelopes.maxpat
  - patches/06-modulation-and-mixing.maxpat
  - patches/07-noise-sculpture-rebuild.maxpat
autonomous: true

must_haves:
  truths:
    - "Every object in every build-up patch has a comment annotation"
    - "Bridge comments appear at each object's first introduction in the series and are blue"
    - "Each patch has 1-2 experiment prompts in visually distinct green bold comment boxes"
    - "Common error points have amber troubleshooting tips"
    - "Color-coded panel regions (blue=Source, orange=Processing, green=Output) group functional sections in every patch"
    - "A color legend comment appears in every patch"
    - "Parameter values are annotated with musical meaning"
  artifacts:
    - path: "patches/01-hello-signal.maxpat"
      provides: "Fully annotated hello signal patch with color regions"
      contains: "panel"
    - path: "patches/04-noise-and-amplitude.maxpat"
      provides: "Fully annotated noise patch with bridge comments for noise~ and experiment prompts"
      contains: "EXPERIMENT"
    - path: "patches/07-noise-sculpture-rebuild.maxpat"
      provides: "Annotated capstone patch with color regions and experiment prompt"
      contains: "panel"
  key_links:
    - from: "patches/01-hello-signal.maxpat"
      to: "Bridge comment for cycle~"
      via: "First introduction of cycle~"
      pattern: "textcolor.*0\\.4.*0\\.4.*0\\.8"
    - from: "patches/05-envelopes.maxpat"
      to: "Bridge comment for line~"
      via: "First introduction of line~"
      pattern: "line~ is the signal version"
---

<objective>
Apply the systematic annotation pass across all 7 build-up patches (01-07). Add color-coded region panels, experiment prompts, bridge comments at first introduction, troubleshooting tips, parameter value annotations, and color legends. This transforms the patches from teaching aids into self-contained study materials students can explore independently.

Purpose: Students take these patches home after lecture. Every patch must be independently understandable — a student opening any single patch should see color-coded regions, experiments to try, troubleshooting help, and clear object explanations without needing to reference other materials.
Output: 7 modified .maxpat files with complete annotation coverage
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-student-handout-and-annotation-polish/04-RESEARCH.md
@patches/01-hello-signal.maxpat
@patches/02-first-tone.maxpat
@patches/03-control-meets-signal.maxpat
@patches/04-noise-and-amplitude.maxpat
@patches/05-envelopes.maxpat
@patches/06-modulation-and-mixing.maxpat
@patches/07-noise-sculpture-rebuild.maxpat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate build-up patches 01-04</name>
  <files>
    patches/01-hello-signal.maxpat
    patches/02-first-tone.maxpat
    patches/03-control-meets-signal.maxpat
    patches/04-noise-and-amplitude.maxpat
  </files>
  <action>
For each of patches 01 through 04, read the existing .maxpat JSON and add the following annotation elements. Follow the exact JSON templates from 04-RESEARCH.md.

**IMPORTANT:** When adding panel objects for color regions, insert them EARLY in the `boxes` array (before the objects they should appear behind) so they render behind the functional objects. All other comment objects can go at the end of the array.

### A. Color-Coded Region Panels (all 4 patches)

Add `panel` objects (maxclass: "panel", mode: 0, rounded: 12) with low-alpha backgrounds to group functional regions:
- **Source region** (oscillators, noise): bgcolor `[0.2, 0.3, 0.55, 0.1]` — covers cycle~, noise~, and their immediate parameter objects
- **Processing region** (envelopes, operators, sig~): bgcolor `[0.55, 0.35, 0.15, 0.1]` — covers *~, +~, sig~, line~ and their control objects
- **Output region** (gain, clip, dac): bgcolor `[0.2, 0.45, 0.25, 0.1]` — covers live.gain~, clip~, dac~/ezdac~, scope

Size each panel to generously enclose its region's objects with ~10-15px padding on each side. Use unique IDs: `obj-region-source`, `obj-region-processing`, `obj-region-output`. Not every patch has all 3 regions — only add panels for regions that contain objects:
- Patch 01: Source (cycle~), Output (live.gain~, clip~, dac~, scope)
- Patch 02: Source (cycle~ objects), Output (live.gain~, clip~, dac~, scope)
- Patch 03: Source (cycle~), Processing (sig~, slider, number~), Output (live.gain~, clip~, dac~)
- Patch 04: Source (noise~, cycle~), Processing (*~ amplitude), Output (live.gain~, clip~, dac~)

### B. Color Legend Comment (all 4 patches)

Add one comment in each patch at the bottom-left corner:
```json
{
    "box": {
        "fontsize": 10.0,
        "id": "obj-color-legend",
        "maxclass": "comment",
        "numinlets": 1,
        "numoutlets": 0,
        "patching_rect": [ 30.0, <bottom_y>, 450.0, 18.0 ],
        "text": "COLOR REGIONS:  Blue = Source (oscillators, noise)  |  Orange = Processing (envelopes, operators)  |  Green = Output (gain, dac~)",
        "textcolor": [ 0.6, 0.6, 0.6, 1.0 ]
    }
}
```
Position `<bottom_y>` about 20px below the lowest existing object in each patch.

### C. Bridge Comments (ONLY at first introduction)

Per user decision: bridge comments appear ONLY at each object's first introduction in the series. Use blue textcolor `[0.4, 0.4, 0.8, 1.0]`. Check what bridge comments already exist (patches 01-03 already have some blue bridge comments per research). Only ADD missing bridge comments, do NOT duplicate existing ones.

First introduction map (from 04-RESEARCH.md Object Inventory):
- **Patch 01:** cycle~, *~, dac~, ezdac~, live.gain~, clip~, live.scope~ — these should all have bridge comments at their first appearance
- **Patch 02:** No new MSP objects introduced (cycle~ frequencies are explored further)
- **Patch 03:** sig~, number~ — need bridge comments
- **Patch 04:** noise~, +~ (brief, full intro in Patch 06) — need bridge comments

Bridge comment examples (use this tone, adapt to each object):
- cycle~: "metro generates bangs at intervals — cycle~ generates a smooth wave, 44,100 samples per second"
- noise~: "random gives one random number per bang — noise~ gives one every sample"
- sig~: "sig~ is the bridge between Max and MSP — it converts a number into a continuous signal"
- number~: "Like a number box but for signals — it samples the audio stream to show you a value"

### D. Experiment Prompts (1-2 per patch)

Add 1-2 experiment prompts per patch as bold green comment boxes. Use this JSON template:
```json
{
    "box": {
        "fontface": 1,
        "fontsize": 11.0,
        "id": "obj-experiment-NN",
        "maxclass": "comment",
        "numinlets": 1,
        "numoutlets": 0,
        "patching_rect": [ x, y, 520.0, 19.0 ],
        "text": "EXPERIMENT: ...",
        "textcolor": [ 0.3, 0.65, 0.3, 1.0 ],
        "bgcolor": [ 0.12, 0.2, 0.12, 1.0 ]
    }
}
```

Per user decision: experiments encourage REWIRING, not just parameter tweaks. Include audio safety caveats when rewiring bypasses the safety chain.

Suggested experiments:
- **Patch 01:** "EXPERIMENT: Disconnect the *~ from cycle~ and send cycle~ directly into live.gain~ — what happens to the waveform on the scope?" (safe — live.gain~ is still in chain). "EXPERIMENT: Change cycle~ from 220 to 20 — can you still hear it? Watch the scope."
- **Patch 02:** "EXPERIMENT: Connect all three cycle~ objects to dac~ through live.gain~ at once — what do you hear when frequencies are close together?"
- **Patch 03:** "EXPERIMENT: Drag the slider while watching number~ — now disconnect sig~ and type a number directly into cycle~'s inlet. What's different?"
- **Patch 04:** "EXPERIMENT: Set the amplitude slider to 0, then slowly bring it up. At what point does the noise become audible?" "EXPERIMENT: Disconnect noise~ from *~ and connect cycle~ instead — you just built Patch 01 again!"

### E. Troubleshooting Tips

Add amber troubleshooting comments near common failure points. Use textcolor `[0.85, 0.6, 0.2, 1.0]`:
- **Patch 01** (near ezdac~): "IF YOU HEAR NOTHING: Is ezdac~ toggled on? Is live.gain~ turned up? Double-click ezdac~ to check Audio Status."
- **Patch 02:** Brief — "See Patch 01 troubleshooting: ezdac~ on, live.gain~ up"
- **Patch 03** (near sig~): "NO SOUND FROM SLIDER? sig~ must be connected — without it, the number never becomes a signal."
- **Patch 04** (near noise~): "WARNING: noise~ at full amplitude is loud! Always start with the amplitude slider low."

### F. Parameter Value Annotations

Add grey `[0.5, 0.5, 0.5, 1.0]` annotations near parameter values that have musical meaning. Only add where missing — check existing annotations first:
- **Patch 01:** "220 = A below middle C" (near cycle~ 220), "0.5 = half amplitude (−6 dB)" (near *~ 0.5)
- **Patch 02:** Frequency labels for comparison tones (if not already present)
- **Patch 03:** "185 = F# below middle C" (near loadbang value), slider range annotation
- **Patch 04:** Noise amplitude meaning

### G. Ensure Every Object Has a Comment

Scan each patch for functional objects (`newobj`, `toggle`, `message`, etc.) that lack any nearby comment annotation. Add terse technical labels in grey `[0.5, 0.5, 0.5, 1.0]` for any unlabeled objects. Later patches (03, 04) should use brief labels with cross-references for returning objects: "cycle~ 330 — cosine oscillator (see Patch 02)".

**ANTI-PATTERN WARNING:** Do NOT over-annotate. If a patch already has 14+ comments and all objects are labeled, focus on adding the NEW annotation types (panels, experiments, troubleshooting, color legend) rather than duplicating existing labels.
  </action>
  <verify>
For each of patches 01-04, verify:
1. Parse JSON successfully (valid .maxpat)
2. At least 2 panel objects exist (Source, Output minimum)
3. Color legend comment exists containing "COLOR REGIONS"
4. At least 1 experiment prompt exists (text contains "EXPERIMENT")
5. Bridge comments exist for objects first introduced in that patch (blue textcolor)
6. At least 1 troubleshooting comment exists in Patch 01 (amber textcolor)
  </verify>
  <done>
Patches 01-04 each have color-coded region panels, a color legend, experiment prompts (1-2 each), bridge comments at first introductions, troubleshooting tips, and parameter annotations. All objects have comment annotations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Annotate build-up patches 05-07</name>
  <files>
    patches/05-envelopes.maxpat
    patches/06-modulation-and-mixing.maxpat
    patches/07-noise-sculpture-rebuild.maxpat
  </files>
  <action>
Apply the same annotation pass to patches 05-07. Same JSON templates, same color scheme, same formatting rules as Task 1.

### A. Color-Coded Region Panels

- **Patch 05:** Source (cycle~), Processing (line~ envelope, toggle/sel/message chain), Output (live.gain~, clip~, dac~)
- **Patch 06:** Source (multiple voices — cycle~, noise~, phasor~), Processing (LFO chain, +~ mixer, master level), Output (live.gain~, clip~, dac~)
- **Patch 07:** Source (3 voices), Processing (automation, envelopes), Output (live.gain~, clip~, dac~). Per decision: Patch 07 annotations are intentionally lighter — reference which patch taught each technique.

### B. Color Legend Comment — same template as Task 1, bottom of each patch.

### C. Bridge Comments (first introductions only)

Objects first introduced in these patches:
- **Patch 05:** line~ — "line~ is the signal version of line — smooth ramps at audio rate instead of message rate." Also toggle, sel, message used in MSP context for the first time — brief bridge notes.
- **Patch 06:** phasor~ — "metro fires bangs at intervals — phasor~ generates a smooth repeating ramp you can use as an LFO." Also +~ gets its full introduction (brief appearance in Patch 04).
- **Patch 07:** No new objects — all are recombinations. Per decision: lighter annotations, reference earlier patches.

Check existing bridge comments before adding — patches 05-06 already have some per research (Patch 05 has line~ bridge, Patch 06 has phasor~ and +~ bridges). Only add what is missing.

### D. Experiment Prompts (1-2 per patch)

Rewiring-style experiments:
- **Patch 05:** "EXPERIMENT: Change the fade-in message from '1. 500' to '1. 2000' — how does a 2-second fade feel different? Now try '1. 50' for a click." (existing TRY THIS may cover this — check and either keep or replace with rewiring-style)
- **Patch 05 (alt):** "EXPERIMENT: Disconnect the line~ from *~ and connect a sig~ with value 0.5 instead — what happens to the envelope?"
- **Patch 06:** "EXPERIMENT: Change phasor~ from 0.3 to 5.0 — hear how the modulation speeds up? Now try 0.01 for a very slow sweep."
- **Patch 06 (alt):** "EXPERIMENT: Disconnect one voice from the +~ mixer — which voice is it? Can you tell by the sound?"
- **Patch 07:** "EXPERIMENT: Open noise-sculpture-demo.maxpat side by side — can you hear the same sound? What looks different in the patching?"

If a patch already has a TRY THIS experiment, check whether it follows the rewiring style. If it only suggests parameter tweaks, replace it with a rewiring-style experiment per user decision. If it already suggests rewiring, keep it but ensure it uses the green bold style (textcolor `[0.3, 0.65, 0.3, 1.0]`, fontface 1, bgcolor `[0.12, 0.2, 0.12, 1.0]`).

### E. Troubleshooting Tips

- **Patch 05** (near toggle): "IF YOU HEAR NOTHING: Toggle must be ON (yellow). Make sure ezdac~ is also on."
- **Patch 06:** Brief — "No sound? Check: ezdac~ on, live.gain~ up, at least one voice toggle on."
- **Patch 07:** "COMPARE: Open the demo patch in a separate window. Toggle ezdac~ in BOTH patches — does Patch 07 sound like the demo?"

### F. Parameter Value Annotations

- **Patch 05:** "0, 1. 500 = ramp from 0 to 1.0 over 500ms (fade in)" / "0 100 = snap to 0 over 100ms (fade out)"
- **Patch 06:** "0.3 Hz = one cycle every ~3 seconds (slow LFO)"
- **Patch 07:** Match demo parameter values — annotate key frequencies and timing values with musical meaning

### G. Object Coverage

Same rule: every functional object gets a comment. Patch 07 uses LIGHTER annotations per project decision — brief labels with cross-references ("line~ envelope — see Patch 05 for full explanation").
  </action>
  <verify>
For each of patches 05-07, verify:
1. Parse JSON successfully (valid .maxpat)
2. At least 2 panel objects exist per patch
3. Color legend comment exists containing "COLOR REGIONS"
4. At least 1 experiment prompt exists (text contains "EXPERIMENT")
5. Bridge comments exist for line~ (Patch 05) and phasor~ (Patch 06)
6. Patch 07 has lighter annotations with cross-references to earlier patches
  </verify>
  <done>
Patches 05-07 each have color-coded region panels, a color legend, experiment prompts, bridge comments at first introductions (05: line~, 06: phasor~), troubleshooting tips, and parameter annotations. Patch 07 uses lighter annotations with cross-references per project decision.
  </done>
</task>

</tasks>

<verification>
1. All 7 build-up patches parse as valid JSON
2. Every patch has at least 2 color region panels (Source + Output minimum)
3. Every patch has a color legend comment
4. Every patch has 1-2 experiment prompts in green bold style
5. Bridge comments exist at each object's first introduction: cycle~/*~/dac~/ezdac~/live.gain~/clip~/live.scope~ (01), sig~/number~ (03), noise~/+~ (04), line~ (05), phasor~ (06)
6. At least patches 01, 03, 04, 05 have troubleshooting tips
7. Parameter values have musical meaning annotations
8. Color scheme is consistent across all 7 patches (same RGBA values for same region types)
</verification>

<success_criteria>
A student opening any single build-up patch sees: colored regions grouping functional sections, a legend explaining the colors, experiments to try, troubleshooting help for common problems, and clear explanations of every object — with bridge comments at first introductions and terse technical labels thereafter.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-handout-and-annotation-polish/04-02-SUMMARY.md`
</output>
