---
phase: 04-student-handout-and-annotation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/student-reference.md
autonomous: true

must_haves:
  truths:
    - "A single-page student reference handout exists listing all MSP objects covered with descriptions and key parameters"
    - "The handout is organized by concept sections, not alphabetically or by patch order"
    - "Each object entry includes name, description, and key parameters"
    - "Phase 3 SVG diagrams are integrated as visual anchors"
    - "The handout lists all 9 patches with one-line descriptions"
  artifacts:
    - path: "docs/student-reference.md"
      provides: "Student reference handout organized by concept sections"
      contains: "cycle~"
  key_links:
    - from: "docs/student-reference.md"
      to: "diagrams/digital-audio-waveform.svg"
      via: "Markdown image reference"
      pattern: "digital-audio-waveform\\.svg"
    - from: "docs/student-reference.md"
      to: "diagrams/signal-flow.svg"
      via: "Markdown image reference"
      pattern: "signal-flow\\.svg"
---

<objective>
Create the student reference handout as a Markdown file organized by concept sections. This single-page reference card gives students everything they need for independent study after the lecture: every MSP object covered, what it does, key parameters, bridge concepts to Max, and a list of all patches.

Purpose: Students need a portable reference they can consult while studying the patches on their own. Cataloging all objects also establishes the definitive inventory for the annotation pass in Plans 02/03.
Output: `docs/student-reference.md`
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-student-handout-and-annotation-polish/04-RESEARCH.md
@diagrams/digital-audio-waveform.svg
@diagrams/signal-flow.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create student reference handout</name>
  <files>docs/student-reference.md</files>
  <action>
Create `docs/student-reference.md` with the following structure. This is a SINGLE-PAGE reference card — keep entries concise, not verbose. Use the Object Inventory from 04-RESEARCH.md as the definitive source for objects and their first-introduction patches.

**Title:** "MSP Quick Reference — MUSC 320 Week 7"

**Sections (organized by concept per user decision, NOT alphabetically):**

1. **What Is a Signal?**
   - Brief explanation: 44,100 samples/second, tilde (~) naming convention, signals vs messages
   - Embed the waveform diagram: `![Digital audio: continuous sound becomes discrete samples](../diagrams/digital-audio-waveform.svg)`

2. **Signal Sources** — objects that generate signals from nothing
   - `cycle~` — cosine/sine oscillator. First argument: frequency in Hz. Connect signal to left inlet for frequency modulation. ("metro generates bangs at intervals; cycle~ generates a smooth wave")
   - `noise~` — white noise generator. No arguments. Outputs random values −1 to 1 at audio rate. ("random gives one random number per bang; noise~ gives one every sample")
   - `phasor~` — repeating 0-to-1 ramp. Frequency in Hz. Used as LFO or automation source. ("metro fires bangs at intervals; phasor~ generates a smooth repeating ramp")

3. **Signal Operators** — objects that transform signals
   - Embed the signal flow diagram: `![Signal flow: Source → Processing → Output](../diagrams/signal-flow.svg)`
   - `*~` — multiplies two signals, or signal by constant. Used for amplitude control, ring modulation, AM. ("You used * to multiply numbers; *~ multiplies signals")
   - `+~` — adds two signals. Used for mixing voices, adding DC offset. ("You added numbers with +; +~ adds signals")
   - `sig~` — converts a Max number to a continuous signal. The bridge between Max and MSP.
   - `line~` — generates smooth ramps at audio rate. Message format: `target time` (e.g., "1. 500" ramps to 1.0 over 500ms). ("line~ is the signal version of line — smooth ramps at audio rate instead of message rate")

4. **Output and Safety** — getting signal to speakers safely
   - `dac~` — digital-to-analog converter. Sends signal to speakers. Two inlets = left/right stereo.
   - `ezdac~` — same as dac~ but with click-to-toggle UI. The power switch for MSP audio.
   - `live.gain~` — volume control with built-in metering. Stereo in/out. Default to −12 dB or lower.
   - `clip~` — limits signal to a range. `clip~ -0.9 0.9` prevents dangerously loud output. Safety net before dac~.

5. **Monitoring** — seeing signals
   - `live.scope~` — oscilloscope display. Shows waveform shape in real time.
   - `number~` — displays signal value at control rate. Mode 2 = display only (no output).

6. **Key Concepts**
   - Signal flow: Source → Processing → Output (always)
   - Bridge objects: sig~ connects the Max world to the MSP world
   - Amplitude matters: always scale signals before output. Full-amplitude noise~ directly to dac~ = dangerously loud
   - The output chain: your signal → live.gain~ → clip~ → dac~ (always use this pattern)
   - DSP must be on: toggle ezdac~ or check Audio Status before expecting sound

7. **Your Patches** — list all 9 patches with one-line descriptions:
   - `01-hello-signal.maxpat` — Your first MSP sound: a test tone through a safe output chain
   - `02-first-tone.maxpat` — Exploring frequency and amplitude with cycle~
   - `03-control-meets-signal.maxpat` — Bridging Max and MSP with sig~ and number~
   - `04-noise-and-amplitude.maxpat` — noise~ as a signal source, controlling amplitude with *~
   - `05-envelopes.maxpat` — Shaping sound over time with line~ and *~
   - `06-modulation-and-mixing.maxpat` — phasor~ automation, +~ mixing, multi-voice architecture
   - `07-noise-sculpture-rebuild.maxpat` — Putting it all together: rebuilding the noise sculpture
   - `noise-sculpture-demo.maxpat` — The finished noise sculpture (what you heard first in class)
   - `three-level-comparison.maxpat` — Control rate vs signal rate vs sample rate (bonus)

**Formatting rules:**
- Use standard Markdown headings (##), bold for object names in running text
- Object entries: `object~` in backticks, em-dash, description, period, Key parameter info in next sentence. Bridge quote in parentheses where applicable.
- Keep the entire document concise enough that it could fit on one printed page (aim for ~100-150 lines of Markdown)
- Image references use relative paths from docs/ to diagrams/: `../diagrams/filename.svg`
  </action>
  <verify>
Verify the handout:
1. File exists at docs/student-reference.md
2. Contains all 14 MSP objects: cycle~, noise~, phasor~, *~, +~, sig~, line~, dac~, ezdac~, live.gain~, clip~, live.scope~, number~
3. Contains both SVG image references (digital-audio-waveform.svg, signal-flow.svg)
4. Contains all 9 patch filenames
5. Organized by concept sections (not alphabetical)
6. Each object has name, description, and key parameters
  </verify>
  <done>
A Markdown student reference handout exists at docs/student-reference.md organized by concept sections, listing all 14 MSP objects with descriptions and parameters, integrating both Phase 3 SVG diagrams, and listing all 9 patches with one-line descriptions.
  </done>
</task>

</tasks>

<verification>
1. `docs/student-reference.md` exists and is valid Markdown
2. All 14 MSP objects appear with descriptions and key parameters
3. Both SVG diagram paths are correct relative references
4. Concept-section organization (not alphabetical, not by patch order)
5. All 9 patch filenames listed with descriptions
6. Document is concise (under 200 lines)
</verification>

<success_criteria>
A student can open the reference handout and find any MSP object covered in the lecture, understand what it does, know its key parameters, see where it fits in the signal flow, and locate which patch demonstrates it.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-handout-and-annotation-polish/04-01-SUMMARY.md`
</output>
