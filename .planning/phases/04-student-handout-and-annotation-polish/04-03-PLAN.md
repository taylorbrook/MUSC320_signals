---
phase: 04-student-handout-and-annotation-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - patches/noise-sculpture-demo.maxpat
  - patches/three-level-comparison.maxpat
autonomous: false

must_haves:
  truths:
    - "The demo patch has color-coded regions, experiment prompts, and parameter annotations in its patching view"
    - "The comparison patch has brief annotations explaining each column"
    - "Color coding is consistent between demo patch and build-up patches"
    - "All 9 patches have been verified by the instructor in Max 9"
  artifacts:
    - path: "patches/noise-sculpture-demo.maxpat"
      provides: "Fully annotated demo patch with color regions and experiments"
      contains: "EXPERIMENT"
    - path: "patches/three-level-comparison.maxpat"
      provides: "Lightly annotated comparison patch"
      contains: "comment"
  key_links:
    - from: "patches/noise-sculpture-demo.maxpat"
      to: "Color region panels"
      via: "Panel objects matching build-up patch color scheme"
      pattern: "bgcolor.*0\\.2.*0\\.3.*0\\.55"
---

<objective>
Annotate the demo patch and comparison patch, then verify all 9 patches with the instructor in Max 9. The demo patch gets full annotation treatment (color regions, experiments, parameter annotations) but NO bridge comments (per convention — bridges only at first introduction in build-up series). The comparison patch gets lighter treatment (brief column explanations only, no color regions, no experiments per research recommendation).

Purpose: Complete the annotation pass across all patches. The demo patch is what students explore AFTER understanding the build-up — experiments here encourage exploring the finished product. The comparison patch is supplementary. Instructor verification ensures all annotations display correctly in Max 9.
Output: 2 modified .maxpat files + instructor sign-off
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-student-handout-and-annotation-polish/04-RESEARCH.md
@.planning/phases/04-student-handout-and-annotation-polish/04-02-SUMMARY.md
@patches/noise-sculpture-demo.maxpat
@patches/three-level-comparison.maxpat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate demo and comparison patches</name>
  <files>
    patches/noise-sculpture-demo.maxpat
    patches/three-level-comparison.maxpat
  </files>
  <action>
### Demo Patch: `noise-sculpture-demo.maxpat`

The demo patch already has Presentation Mode UI labels (~15 comment objects) and 2 panel objects for Presentation Mode backgrounds. The PATCHING VIEW (where students study the signal flow) needs the annotation treatment. Be careful NOT to interfere with existing Presentation Mode elements.

**A. Color-Coded Region Panels (patching view only)**

Add panels for the three voice groups + output chain. The demo has 3 voices, so the Source region may need multiple panels or one large one. Recommended approach:
- **Source region** (all 3 voice oscillators/noise): bgcolor `[0.2, 0.3, 0.55, 0.1]` — one panel per voice, or one large panel if voices are spatially grouped
- **Processing region** (automation engine — phasor~ chains, line~ ramps, *~ amplitude): bgcolor `[0.55, 0.35, 0.15, 0.1]`
- **Output region** (mix bus, live.gain~, clip~, dac~): bgcolor `[0.2, 0.45, 0.25, 0.1]`

Insert panel objects EARLY in the boxes array. Ensure they do NOT have `presentation: 1` — they should only appear in patching view, not disrupt the existing Presentation Mode layout.

**B. Color Legend Comment** — same template as build-up patches, bottom of patching view.

**C. NO Bridge Comments** — the demo is not where objects are introduced. Students see bridge comments in the build-up series.

**D. Experiment Prompts (1-2)**

The demo has sliders in Presentation Mode. Experiments should encourage exploring the finished product:
- "EXPERIMENT: Move the Mix slider all the way to one side, then the other. Which side is pure noise? Which is pure tone? Now find a blend you like."
- "EXPERIMENT: Try the Rate slider at minimum and maximum. How does automation speed change the character of the sound?"

Use same green bold template (textcolor `[0.3, 0.65, 0.3, 1.0]`, fontface 1, bgcolor `[0.12, 0.2, 0.12, 1.0]`). Place in patching view near the sliders.

**E. Parameter Annotations**

Annotate key parameter values visible in the patching view:
- Metro interval and its relationship to total automation cycle time
- Line~ ramp ranges and what they control (frequency range, amplitude range)
- Voice label colors (existing: Noise=red, Tone=blue, Drone=amber) — add brief text labels near each voice section header if not already present

**F. Object Labels**

The demo's patching view currently has Presentation Mode labels but may lack patching-view annotations. Add terse technical labels for objects in the signal path. No need for extensive explanations — students will have studied the build-up series first. Focus on: "Voice 1: Noise — noise~ through phasor~ amplitude modulation" style section headers.

### Comparison Patch: `three-level-comparison.maxpat`

Per research recommendation: LIGHTER treatment. This is a visual aid, not a study patch.

**A. NO color region panels** — skip for this patch
**B. NO experiment prompts** — skip for this patch

**C. Brief Column Annotations**

Add/ensure comments exist for each of the three columns:
- Column 1 (Control rate): "CONTROL RATE: metro sends bangs at message rate — one value per bang, like a dripping faucet"
- Column 2 (Signal rate): "SIGNAL RATE: phasor~ generates a continuous ramp — 44,100 values per second, smooth flow"
- Column 3 (Sample rate): "SAMPLE RATE: gen~ counter increments every single sample — the fastest possible rate in Max"

**D. Brief Object Labels**

Ensure each functional object has a terse label. No bridge comments (this patch is supplementary, not introductory).
  </action>
  <verify>
1. Demo patch: Parse JSON successfully
2. Demo patch: At least 2 panel objects with region bgcolor values exist (not counting existing Presentation Mode panels)
3. Demo patch: Color legend comment exists
4. Demo patch: At least 1 experiment prompt (text contains "EXPERIMENT")
5. Demo patch: No bridge comments (blue textcolor) added — only existing Presentation Mode labels
6. Comparison patch: Parse JSON successfully
7. Comparison patch: Column explanation comments exist for all 3 columns
8. Comparison patch: No panel region objects added
  </verify>
  <done>
Demo patch has color-coded regions, experiment prompts, parameter annotations, and a color legend in patching view without disrupting Presentation Mode. Comparison patch has brief column explanations and object labels. No bridge comments in either patch.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Instructor verification of all annotated patches in Max 9</name>
  <files>
    patches/noise-sculpture-demo.maxpat
    patches/three-level-comparison.maxpat
    patches/01-hello-signal.maxpat
    patches/02-first-tone.maxpat
    patches/03-control-meets-signal.maxpat
    patches/04-noise-and-amplitude.maxpat
    patches/05-envelopes.maxpat
    patches/06-modulation-and-mixing.maxpat
    patches/07-noise-sculpture-rebuild.maxpat
  </files>
  <action>
Present the complete annotation pass to the instructor for verification in Max 9.

What was built:
- 7 build-up patches with color-coded regions, experiment prompts, bridge comments, troubleshooting tips, parameter annotations, and color legends
- Demo patch with color regions, experiments, and parameter annotations (no bridge comments)
- Comparison patch with brief column annotations (lighter treatment)
- Student reference handout at docs/student-reference.md

All patches should display:
- Blue-tinted panels behind Source regions (oscillators, noise)
- Orange-tinted panels behind Processing regions (envelopes, operators)
- Green-tinted panels behind Output regions (gain, dac~)
- Green bold experiment prompts with dark green background
- Amber troubleshooting tips
- Blue bridge comments at first introduction of each object
- Grey color legend at the bottom of each patch

Verification steps for instructor:

1. Color regions visible: In each patch, verify the tinted background panels are visible behind object groups. If too faint (alpha 0.1), report and we can increase alpha. Panels should NOT appear in the demo's Presentation Mode.

2. Experiment prompts readable: Green bold text with dark green background should stand out from regular annotations. Check 1-2 per patch.

3. Bridge comments appropriate: Blue comments should appear at an object's FIRST introduction only (01: cycle~, *~, dac~, etc.; 03: sig~, number~; 04: noise~; 05: line~; 06: phasor~). Later patches should have grey technical labels, not blue bridge comments.

4. Troubleshooting tips placed well: Amber text near common failure points (ezdac~ toggle, live.gain~ level, sig~ connection).

5. No Presentation Mode disruption: Open the demo patch in Presentation Mode — it should look exactly as before (no new panels or annotations visible in Presentation Mode).

6. Handout accuracy: Open docs/student-reference.md and spot-check: are all 14 MSP objects listed? Do descriptions match what the objects actually do in the patches?

7. Color consistency: The blue/orange/green region scheme should look the same across all patches.
  </action>
  <verify>Instructor types "approved" or describes issues to fix</verify>
  <done>All 9 patches and the student reference handout have been verified by the instructor in Max 9. Phase 4 is complete.</done>
</task>

</tasks>

<verification>
1. All 9 patches parse as valid JSON and open in Max 9 without errors
2. Demo patch has color regions in patching view without disrupting Presentation Mode
3. Comparison patch has brief annotations without color regions
4. Color scheme is consistent across all 9 patches (same RGBA values)
5. Instructor has verified the complete annotation suite in Max 9
6. Student reference handout exists and is accurate
</verification>

<success_criteria>
All 9 patches are fully annotated, color-coded, and verified in Max 9. The student reference handout is complete. A student can take the entire kit (9 patches + handout + diagrams) and study MSP independently, with each patch serving as a self-contained, visually organized lesson.
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-handout-and-annotation-polish/04-03-SUMMARY.md`
</output>
