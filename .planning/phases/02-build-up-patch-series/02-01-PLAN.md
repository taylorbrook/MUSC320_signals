---
phase: 02-build-up-patch-series
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/01-hello-signal.maxpat
  - patches/02-first-tone.maxpat
  - patches/03-control-meets-signal.maxpat
autonomous: true

must_haves:
  truths:
    - "Opening 01-hello-signal.maxpat produces a 440 Hz tone when ezdac~ is clicked"
    - "Opening 02-first-tone.maxpat lets the student hear tones at different frequencies and amplitudes"
    - "Opening 03-control-meets-signal.maxpat lets the student move a slider and hear pitch change via sig~"
    - "Each patch works without any other patch open -- fully self-contained"
    - "Each patch shows the signal on a live.scope~ display"
  artifacts:
    - path: "patches/01-hello-signal.maxpat"
      provides: "TOPC-03: engaging signals with dac~, ezdac~, cycle~ test tone, output safety chain"
      contains: "ezdac~"
    - path: "patches/02-first-tone.maxpat"
      provides: "TOPC-04 partial: cycle~ frequency/amplitude parameters, *~ as amplitude control"
      contains: "cycle~"
    - path: "patches/03-control-meets-signal.maxpat"
      provides: "TOPC-02: sig~ and number~ bridging control and signal domains"
      contains: "sig~"
  key_links:
    - from: "patches/01-hello-signal.maxpat"
      to: "dac~"
      via: "cycle~ 440 -> *~ 0.5 -> live.gain~ -> clip~ -> dac~"
      pattern: "cycle~.*dac~"
    - from: "patches/03-control-meets-signal.maxpat"
      to: "cycle~ frequency inlet"
      via: "slider -> sig~ -> cycle~ left inlet"
      pattern: "sig~"
---

<objective>
Create the first three build-up patches (01-hello-signal, 02-first-tone, 03-control-meets-signal) covering TOPC-02, TOPC-03, and TOPC-04 (partial).

Purpose: These three patches establish the MSP foundation -- students learn how to hear audio (dac~/ezdac~), what makes a tone (cycle~, frequency, amplitude), and how to bridge Max controls into the signal domain (sig~, number~). Every subsequent patch builds on these concepts.

Output: Three self-contained .maxpat files with detailed pedagogical annotations, each producing audible output and including the standard output safety chain (live.gain~ -> clip~ -> dac~).
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-up-patch-series/02-RESEARCH.md
@.planning/phases/01-noise-sculpture-demo/01-01-SUMMARY.md
@patches/noise-sculpture-demo.maxpat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 01-hello-signal.maxpat -- the MSP "hello world"</name>
  <files>patches/01-hello-signal.maxpat</files>
  <action>
Create a self-contained .maxpat file implementing the simplest possible MSP patch that produces audible output.

**Signal chain:**
cycle~ 440 -> *~ 0.5 -> live.gain~ (stereo, -12 dB default) -> clip~ -0.9 0.9 (stereo pair) -> dac~

**Additional objects:**
- ezdac~ (visible, click to toggle DSP -- DO NOT auto-start DSP with loadbang)
- live.scope~ tapped at cycle~ output (before *~ 0.5) showing the clean cosine waveform

**live.gain~ configuration (match demo pattern):**
- parameter_enable: 1
- parameter_initial: [-12.0], parameter_initial_enable: 1
- parameter_longname: "Build-01 Vol" (unique per patch to avoid parameter collision -- see Pitfall 2 in research)
- parameter_mmax: 6.0, parameter_mmin: -70.0, parameter_unitstyle: 4
- Stereo: numinlets 2, numoutlets 5, outlettype ["signal","signal","","float","list"]

**Layout (top to bottom, left to right):**
- Title comment at top: "01 — Hello Signal" (fontface 1, fontsize 16)
- Subtitle: "How do I hear anything from MSP?" (fontsize 12)
- Section: cycle~ 440 -> *~ 0.5 in the center
- live.scope~ to the right of the signal chain
- Output chain at bottom: live.gain~ -> clip~ stereo -> dac~
- ezdac~ near dac~ (visible, clickable)

**Comment annotations (the primary teaching content -- place to the right of or below each object):**
- "cycle~ 440 -- generates a cosine wave at 440 Hz (A above middle C). MSP processes this as a stream of 44,100 numbers per second."
- "*~ 0.5 -- multiplies every sample by 0.5, cutting the amplitude in half. Without this, the tone would be at full volume."
- "ezdac~ -- click this to start/stop audio processing. This is the MSP power switch."
- "dac~ -- digital-to-analog converter. Sends the signal to your speakers. Double-click to see Audio Status."
- "live.gain~ -- master volume control with metering. Set to -12 dB for safe listening."
- "clip~ -0.9 0.9 -- safety limiter. Prevents any signal from exceeding safe amplitude."
- "live.scope~ -- oscilloscope display. Shows the waveform so you can see what you hear."
- TOPC-01 annotation block (group of comments near the top): "WHAT IS A SIGNAL? In Max, values update only when a message arrives. In MSP, signals flow continuously -- 44,100 samples every second. That's what the ~ means: this object works with signals, not messages."
- TOPC-03 annotation: "Audio Status: double-click dac~ to see the Audio Status panel. It shows sample rate, I/O vector size, and which audio driver is active."
- Mention but do NOT wire adc~/ezadc~: "adc~ is the input equivalent of dac~ -- it brings microphone audio INTO Max. We won't use it today."

**JSON structure:** Use fileversion 1, appversion 9.1.1, classnamespace "box", rect [100, 100, 900, 700]. NO openinpresentation (patching view by default). Include description: "01 — Hello Signal: engaging signals with dac~, ezdac~, and your first tone (MUSC 320 Week 7)".

**Object IDs:** Use descriptive IDs (obj-cycle, obj-amp, obj-gain, obj-clipL, obj-clipR, obj-dac, obj-ezdac, obj-scope, obj-title, etc.) for maintainability.
  </action>
  <verify>
Validate JSON: `python3 -c "import json; json.load(open('patches/01-hello-signal.maxpat')); print('valid JSON')"`. Verify all required objects exist: cycle~, *~, live.gain~, clip~ (x2), dac~, ezdac~, live.scope~. Verify signal chain connectivity: cycle~ outlet 0 connects to *~ inlet 0, *~ connects to live.gain~ inlet 0 and inlet 1, live.gain~ outlets 0/1 connect to clip~ inlets, clip~ outlets connect to dac~ inlets.
  </verify>
  <done>01-hello-signal.maxpat is valid JSON, contains all required objects with complete signal chain from cycle~ 440 to dac~ via the safety chain, has 8+ comment annotations covering TOPC-01 and TOPC-03, and live.gain~ has unique parameter_longname "Build-01 Vol".</done>
</task>

<task type="auto">
  <name>Task 2: Create 02-first-tone.maxpat -- frequency and amplitude</name>
  <files>patches/02-first-tone.maxpat</files>
  <action>
Create a self-contained .maxpat teaching cycle~ parameters and *~ as amplitude control.

**Main signal chain (produces sound):**
cycle~ 440 -> *~ 0.5 -> live.gain~ -> clip~ stereo -> dac~ + ezdac~

**Comparison section (visual only, separate scope displays -- not connected to output):**
- Three cycle~ objects side by side with different frequencies: cycle~ 220, cycle~ 440, cycle~ 880
- Each connected to its own live.scope~ to show how frequency changes waveform density
- Label comments: "220 Hz -- one octave below A4", "440 Hz -- A4 (concert pitch)", "880 Hz -- one octave above A4"
- These comparison cycle~ objects connect ONLY to their scope~ displays, NOT to the output chain. They are visual demonstrations only.

**Amplitude comparison (on the main tone):**
- Show *~ with different arguments annotated: "Try changing 0.5 to 0.25 (quieter) or 1.0 (full amplitude)"
- Comment: "*~ is a signal multiplier. It scales every sample. 0.5 = half amplitude, 0.25 = quarter amplitude."

**Output chain:** Same pattern as Patch 01 -- live.gain~ (parameter_longname "Build-02 Vol") -> clip~ stereo -> dac~

**live.scope~ displays:**
- 1 scope on the main output (cycle~ 440 output, before *~)
- 3 smaller scopes on the comparison section

**Layout:**
- Title: "02 — First Tone" (fontface 1, fontsize 16)
- Subtitle: "What makes that tone? Can I change it?"
- Main signal chain on the left
- Frequency comparison section on the right
- Output chain at bottom

**Comment annotations:**
- "cycle~ -- cosine wave oscillator. The number after it is the frequency in Hertz (cycles per second)."
- "440 Hz = A above middle C. Double the frequency = up one octave."
- "*~ 0.5 -- amplitude scaling. Multiply every sample by 0.5 to cut the volume in half."
- "Why not just turn it up to 1.0? In MSP, managing amplitude is critical. Full-amplitude signals from multiple sources can clip (distort). Always scale down."
- "live.scope~ shows the waveform. Notice: 880 Hz has twice as many cycles on screen as 440 Hz."
- Reference to Patch 01: "ezdac~, dac~, live.gain~, clip~ -- see Patch 01 for details"

**JSON structure:** fileversion 1, appversion 9.1.1, rect [100, 100, 1000, 750] (slightly wider for comparison section). Description: "02 — First Tone: cycle~ frequency and amplitude, *~ scaling (MUSC 320 Week 7)".
  </action>
  <verify>
Validate JSON. Verify objects: cycle~ (x4), *~ (at least 1 in signal chain), live.gain~, clip~ (x2), dac~, ezdac~, live.scope~ (x4). Verify main signal chain connected to output. Verify comparison cycle~ objects connect only to their scopes, NOT to dac~.
  </verify>
  <done>02-first-tone.maxpat is valid JSON, has a main audible signal chain plus a visual frequency comparison section with 3 separate scopes, all annotations present, live.gain~ has unique parameter_longname "Build-02 Vol".</done>
</task>

<task type="auto">
  <name>Task 3: Create 03-control-meets-signal.maxpat -- sig~ and number~ bridge</name>
  <files>patches/03-control-meets-signal.maxpat</files>
  <action>
Create a self-contained .maxpat teaching sig~ and number~ as the bridge between Max control domain and MSP signal domain. This is the TOPC-02 patch -- the core conceptual lesson.

**Signal chain (produces sound):**
slider (floatoutput=1, min=80, size=320) -> sig~ -> cycle~ (left/frequency inlet) -> *~ 0.5 -> live.gain~ -> clip~ stereo -> dac~ + ezdac~

Use the established slider pattern from Phase 1: slider with floatoutput=1, min and size properties for direct range control. NO /127. scaling, NO pack/line~ ramping.

**Side-by-side comparison (the teaching centerpiece):**

LEFT SIDE -- "Control Domain (you know this)":
- Same slider (connect slider output to both sides)
- slider -> number box (standard Max number, showing integer/float value)
- Comment: "The number box updates only when you move the slider."

RIGHT SIDE -- "Signal Domain (new)":
- Same slider -> sig~ -> number~ (mode 2, display only)
- Comment: "sig~ converts this number into a continuous signal. number~ shows its value, updating ~4 times per second."
- sig~ output also fans to cycle~ left inlet (controlling frequency)
- number~ is mode=2 (signal display, not signal generator)

**Visual feedback:**
- live.scope~ tapped at cycle~ output showing waveform changing as slider moves
- Comment near scope: "Watch: move the slider and see the waveform change. Higher pitch = more cycles on screen."

**Output chain:** live.gain~ (parameter_longname "Build-03 Vol") -> clip~ stereo -> dac~

**Loadbang initialization:**
- loadbang -> message "185." -> slider (sets initial frequency to 185 Hz so patch produces sound immediately when ezdac~ clicked)
- Do NOT auto-start DSP

**Layout:**
- Title: "03 — Control Meets Signal" (fontface 1, fontsize 16)
- Subtitle: "How do I connect Max controls to MSP signals?"
- Side-by-side comparison in the upper half, clearly divided
- Section labels: "CONTROL DOMAIN" and "SIGNAL DOMAIN" (fontface 1)
- Signal chain below the comparison
- Output chain at bottom

**Comment annotations:**
- TOPC-02 central annotation: "In Max, the number box updates only when you change the slider. In MSP, sig~ continuously outputs the value at 44,100 samples per second -- that's what the ~ means."
- "sig~ -- converts a single Max number into a continuous audio-rate signal. This is the bridge between Max and MSP."
- "number~ (mode 2) -- displays the current value of a signal. Updates about 4 times per second (not at audio rate -- that would be too fast to read)."
- "This slider controls frequency in Hz. Move it and hear the pitch change in real time."
- "Why sig~ instead of connecting the slider directly? Signal inlets need a signal input. sig~ provides that continuous stream."
- Reference: "cycle~, *~, output chain -- see Patches 01-02 for details"

**JSON structure:** fileversion 1, appversion 9.1.1, rect [100, 100, 1000, 750]. Description: "03 — Control Meets Signal: sig~ and number~ bridge the control-signal divide (MUSC 320 Week 7)".
  </action>
  <verify>
Validate JSON. Verify objects: slider (floatoutput=1), sig~, number~ (mode=2), number (standard), cycle~, *~, live.gain~, clip~ (x2), dac~, ezdac~, live.scope~, loadbang. Verify slider connects to both number box (control) and sig~ (signal). Verify sig~ connects to cycle~ left inlet. Verify loadbang -> message -> slider chain exists.
  </verify>
  <done>03-control-meets-signal.maxpat is valid JSON, has side-by-side control/signal comparison with slider driving both paths, sig~ controlling cycle~ frequency, number~ in display mode, loadbang sets 185 Hz default, all annotations present covering TOPC-02, live.gain~ has unique parameter_longname "Build-03 Vol".</done>
</task>

</tasks>

<verification>
For each of the three patches:
1. Valid JSON: `python3 -c "import json; json.load(open('patches/NN-name.maxpat')); print('OK')"`
2. Contains complete signal chain to dac~ (no silent patches -- BILD-03)
3. Contains live.gain~ with unique parameter_longname (Pitfall 2)
4. Contains clip~ stereo pair (BILD-05)
5. Contains live.scope~ (BILD-06)
6. Contains ezdac~ (BILD-02 -- self-contained)
7. Contains 5+ comment annotations
8. No objects used before their introduction patch (strict object budget -- Pitfall 5):
   - Patch 01 may use: dac~, ezdac~, cycle~, *~, live.gain~, clip~, live.scope~, comment
   - Patch 02 adds: no new MSP objects (deeper exploration of cycle~, *~)
   - Patch 03 adds: sig~, number~, slider, number, loadbang
</verification>

<success_criteria>
- Three .maxpat files exist in patches/ directory, each valid JSON
- Each patch produces audible output via a complete signal chain ending at dac~
- Each patch has a unique live.gain~ parameter_longname
- TOPC-02 covered by Patch 03 (sig~/number~ comparison)
- TOPC-03 covered by Patch 01 (dac~/ezdac~/Audio Status)
- TOPC-04 partially covered by Patch 02 (cycle~ parameters, *~ amplitude)
- No patch uses MSP objects not yet introduced in the series order
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-up-patch-series/02-01-SUMMARY.md`
</output>
