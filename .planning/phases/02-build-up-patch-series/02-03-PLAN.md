---
phase: 02-build-up-patch-series
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - patches/07-noise-sculpture-rebuild.maxpat
autonomous: false

must_haves:
  truths:
    - "Opening 07-noise-sculpture-rebuild.maxpat and clicking ezdac~ produces a sound that audibly resembles the demo patch"
    - "The rebuild uses the same phasor~ rates, voice levels, and automation scaling as noise-sculpture-demo.maxpat"
    - "All 7 build-up patches open without error and produce audible output"
    - "The 5 required topics (TOPC-01 through TOPC-05) are each covered by at least one patch"
  artifacts:
    - path: "patches/07-noise-sculpture-rebuild.maxpat"
      provides: "BILD-04: complete noise sculpture rebuild from all concepts in patches 01-06"
      contains: "phasor~"
  key_links:
    - from: "patches/07-noise-sculpture-rebuild.maxpat"
      to: "patches/noise-sculpture-demo.maxpat"
      via: "identical phasor~ rates, voice architecture, mixing coefficients"
      pattern: "phasor~ 0\\.071"
---

<objective>
Create the final build-up patch (07-noise-sculpture-rebuild) that combines all concepts from patches 01-06 to recreate the demo, then verify all 7 patches work correctly.

Purpose: This is the capstone -- students see that everything they learned across 6 patches assembles into the opening demo. The rebuild uses the exact same parameter values as the demo patch to ensure sonic match (BILD-04). Human verification confirms the complete series works.

Output: One .maxpat file matching the demo's signal architecture, plus human verification that all 7 patches produce correct audible output.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-build-up-patch-series/02-RESEARCH.md
@.planning/phases/01-noise-sculpture-demo/01-01-SUMMARY.md
@.planning/phases/01-noise-sculpture-demo/01-02-SUMMARY.md
@.planning/phases/02-build-up-patch-series/02-01-SUMMARY.md
@.planning/phases/02-build-up-patch-series/02-02-SUMMARY.md
@patches/noise-sculpture-demo.maxpat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 07-noise-sculpture-rebuild.maxpat -- complete demo reconstruction</name>
  <files>patches/07-noise-sculpture-rebuild.maxpat</files>
  <action>
Create a self-contained .maxpat that recreates the noise sculpture demo using all concepts from patches 01-06. This patch must use the EXACT same parameter values as noise-sculpture-demo.maxpat to satisfy BILD-04 (audibly resembles the demo).

**Read patches/noise-sculpture-demo.maxpat carefully and replicate the signal architecture.**

**3-Voice architecture (replicate from demo):**

Voice 1 -- Rhythmic Noise:
- noise~ -> *~ (gated by unipolar LFO) -> *~ (amplitude automation) -> *~ (master level)
- LFO chain: phasor~ rate from slider 1 -> sig~ -> +~ -> cycle~ (frequency modulated by phasor~ 0.071 -> cycle~ 0 -> *~ 1.5 automation) -> *~ 0.5 -> +~ 0.5 (unipolar)
- Amplitude: phasor~ 0.053 -> cycle~ 0 -> *~ 0.45 -> +~ 0.5 (unipolar)
- Voice level: *~ by slider 3 mix value (via sig~)

Voice 2 -- Tonal AM / Beating:
- cycle~ (frequency from slider 2 -> sig~ + freq automation from phasor~ 0.041 -> cycle~ 0 -> *~ 20)
- Detuned carrier: same freq sig~ -> +~ 2 -> cycle~ (2 Hz detuning)
- Two carriers multiplied: *~ (ring modulation)
- Amplitude: phasor~ 0.062 -> cycle~ 0 -> *~ 0.45 -> +~ 0.5 (unipolar)
- Voice level: *~ by inverted slider 3 mix value (slider -> !- 1. -> sig~)

Voice 3 -- Evolving Drone:
- cycle~ (frequency from phasor~ 0.031 -> cycle~ 0 -> *~ 25 -> +~ 110 base freq automation)
- Detuned partial: same freq + slider 4 detune (via sig~ -> +~) -> cycle~ -> *~ 0.7
- Two partials summed: +~
- Amplitude: phasor~ 0.047 -> cycle~ 0 -> *~ 0.4 -> +~ 0.45 (unipolar)
- Voice level: *~ by inverted slider 3 mix value (same as V2)

**Exact phasor~ rates (from demo -- do NOT approximate):**
- 0.071 (V1 LFO rate modulation)
- 0.053 (V1 amplitude)
- 0.041 (V2 frequency modulation)
- 0.062 (V2 amplitude)
- 0.031 (V3 base frequency)
- 0.047 (V3 amplitude)

**4 Performance sliders (matching demo):**
1. Noise Speed: slider (floatoutput=1, size=8.0) -> sig~ -> V1 LFO rate
2. Tone Pitch: slider (floatoutput=1, min=80, size=320) -> sig~ -> V2 cycle~ freq
3. Noise/Tone Mix: slider (floatoutput=1, size=1.0) -> sig~ for V1 level, also -> !- 1. -> sig~ for V2+V3 level
4. Drone Detune: slider (floatoutput=1, size=10.0) -> sig~ -> V3 detune offset

**Master on/off:**
toggle -> sel 0 1
- ON: message "0, 1. 2000" -> line~ (2-second fade in)
- OFF: message "0 100" -> line~ (100ms fade out)
- line~ -> *~ on the mixed signal (master level gate)

**Mix bus:**
Voice 1 output + Voice 2 output -> +~ -> + Voice 3 output -> +~ -> *~ (master level) -> live.gain~ -> clip~ stereo -> dac~ + ezdac~

**Loadbang defaults (matching demo):**
loadbang -> t b b b b -> messages: "2." (slider 1), "185." (slider 2), "0.5" (slider 3), "3." (slider 4)

**live.gain~ configuration:**
- parameter_longname: "Build-07 Vol" (unique)
- parameter_initial: [-12.0], parameter_initial_enable: 1
- All other parameters matching demo pattern

**Scope displays (4+):**
- live.scope~ on main output (before live.gain~)
- live.scope~ on V1 (noise) output
- live.scope~ on V2 (tone) output
- live.scope~ on V3 (drone) output

**No Presentation Mode.** Patch 07 is the "under the hood" view -- students compare this patching view to the demo's polished Presentation Mode. The demo IS the Presentation Mode experience.

**Layout:**
- Title: "07 — Noise Sculpture Rebuild" (fontface 1, fontsize 16)
- Subtitle: "We built everything in the demo -- from scratch."
- Master on/off section at top
- Phasor automation section below (6 phasor~ chains, matching demo layout)
- 3 voice sections in a row (V1 left, V2 center, V3 right)
- Sliders section below voices
- Mix bus and output chain at bottom
- Use same section labels as demo: "=== VOICE 1: Rhythmic Noise ===", etc.

**Comment annotations (lighter than patches 01-06 -- students already know the concepts):**
- "This patch combines everything from Patches 01-06 into the complete noise sculpture."
- Brief labels on each voice section referencing which patch taught the concept
- "Voice 1 uses noise~ (Patch 04) with phasor~ LFO modulation (Patch 06)"
- "Voice 2 uses ring modulation: two detuned cycle~ multiplied (Patches 02, 06)"
- "Voice 3 uses additive synthesis: two detuned cycle~ summed (Patches 02, 06)"
- "The mix slider uses !- 1. -- a control-domain trick: one slider controls noise level (direct) and tone+drone level (inverted)"
- "Compare this patch to the demo: same sound, same objects, different layout. The demo adds Presentation Mode polish."
- "TRY THIS: open the demo patch side by side and compare. Switch ezdac~ between them -- hear the same sound?"

**JSON structure:** fileversion 1, appversion 9.1.1, rect [100, 100, 1100, 1200] (tall for 3-voice layout). Description: "07 — Noise Sculpture Rebuild: complete demo reconstruction from all concepts (MUSC 320 Week 7)".
  </action>
  <verify>
Validate JSON: `python3 -c "import json; json.load(open('patches/07-noise-sculpture-rebuild.maxpat')); print('valid JSON')"`. Verify: 6 phasor~ objects with correct rates (0.071, 0.053, 0.041, 0.062, 0.031, 0.047). Verify 3 noise~/cycle~ voice sections. Verify 4 sliders with sig~ connections. Verify mix bus connects all voices to output chain. Verify loadbang defaults match demo (2., 185., 0.5, 3.). Verify live.gain~ parameter_longname is "Build-07 Vol".
  </verify>
  <done>07-noise-sculpture-rebuild.maxpat is valid JSON, contains complete 3-voice architecture with exact demo parameter values, 6 phasor~ automation chains, 4 sliders, toggle master on/off, loadbang defaults matching demo, 4+ scopes, annotations referencing earlier patches, live.gain~ has unique parameter_longname.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 7 build-up patches in Max 9</name>
  <files>patches/01-hello-signal.maxpat, patches/02-first-tone.maxpat, patches/03-control-meets-signal.maxpat, patches/04-noise-and-amplitude.maxpat, patches/05-envelopes.maxpat, patches/06-modulation-and-mixing.maxpat, patches/07-noise-sculpture-rebuild.maxpat</files>
  <action>
Human verification checkpoint. Claude has built the complete 7-patch build-up series deconstructing the noise sculpture demo into a one-concept-per-patch progression. Each patch is self-contained with its own output safety chain and pedagogical annotations. The user must open each patch in Max 9 and verify it produces correct audible output.
  </action>
  <verify>
Open each patch in Max 9 and verify audible output:

1. **01-hello-signal.maxpat** -- Click ezdac~. Hear a steady 440 Hz tone? See the cosine waveform on scope?

2. **02-first-tone.maxpat** -- Click ezdac~. Hear the tone? Do the 3 comparison scopes show different waveform densities (220/440/880)?

3. **03-control-meets-signal.maxpat** -- Click ezdac~. Move the slider. Does pitch change smoothly? Do both number box and number~ display values?

4. **04-noise-and-amplitude.maxpat** -- Click ezdac~. Hear noise alongside a tone? Does the slider control noise volume? Do the two scopes show noise vs tone contrast?

5. **05-envelopes.maxpat** -- Click ezdac~, then toggle ON. Does the tone fade in? Toggle OFF -- does it fade out? Do the scopes show the envelope shape?

6. **06-modulation-and-mixing.maxpat** -- Click ezdac~, then toggle ON. Hear the tremolo tone (Voice A) and beating drone (Voice B) together? Do the scopes show the LFO and voice signals?

7. **07-noise-sculpture-rebuild.maxpat** -- Click ezdac~, then toggle ON. Does it sound like the demo? Open noise-sculpture-demo.maxpat and compare side by side.

**Also verify:**
- No error messages in Max console when opening any patch
- No parameter name collision warnings when multiple patches are open simultaneously
- Comments/annotations are readable and pedagogically clear
  </verify>
  <done>All 7 patches produce correct audible output in Max 9. Patch 07 audibly resembles the demo. No errors, no parameter collisions. User approves the complete build-up series.</done>
</task>

</tasks>

<verification>
Phase 2 success criteria (from ROADMAP.md):
1. Opening any single build-up patch works without errors -> verified by human in Max 9
2. Every build-up patch produces audible output when DSP is on -> verified patch by patch
3. The five required topics are each clearly covered:
   - TOPC-01 (digital audio basics): Patch 01 annotations + Patch 06 phasor~ reinforcement
   - TOPC-02 (signal vs control): Patch 03 side-by-side comparison
   - TOPC-03 (engaging signals): Patch 01 dac~/ezdac~/Audio Status
   - TOPC-04 (basic operators): Patches 02 + 03 + 04 (cycle~, *~, sig~, number~, noise~, live.gain~, +~)
   - TOPC-05 (envelopes): Patch 05 line~ with *~
4. Final build-up patch audibly resembles the demo -> Patch 07 A/B comparison
5. Every patch includes gain~/limiter and scope~ -> verified per patch
</verification>

<success_criteria>
- 07-noise-sculpture-rebuild.maxpat exists and is valid JSON
- Patch 07 uses exact phasor~ rates and voice architecture from the demo
- Human verifies all 7 patches produce correct audible output in Max 9
- Human confirms Patch 07 audibly resembles the demo
- No parameter name collisions across patches
</success_criteria>

<output>
After completion, create `.planning/phases/02-build-up-patch-series/02-03-SUMMARY.md`
</output>
